line-length = 100
target-version = "py39"

[lint]
select = ["E", "F", "I", "W", "UP"]
ignore = ["E501"] # Игнорируем слишком длинные строки, если они читаемы

[lint.isort]
known-first-party = ["app", "tests"]